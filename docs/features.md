# Home page

Add a home page that:

1. Show the asset that the user is currently possessing ( based on the user comany email which will be updated later on the database)

2. Add a button that allow the user to make requests: (For each type of request create a form according to the company guidline that can be exported to pdf )

- Move asset from A to B or recieve asset from X ( invidiual/ department/ company branches). 
- Acquire new asset ( for individual/ department/ company branches/ projects)
- Liquidify asset

# Import/ Export

- Add buttons to the asset page that allows for import/export the asset list into either excel or pdf files.

- The user use the filter to show the desiredd asset. Then the user can mark desired items to export or export the whole page.
